platform :ios, '7.0'

# Read pod from remote repository
pod 'MSSData', '~> 1.0.3'

# Read pod directly from repo on local disk - useful while developing the pod itself
#pod 'MSSData', :path => "../data-ios"
